// app/layout.tsx
import type { Metadata } from "next";
import { getInitialThemeScript } from '@/lib/setInitialTheme'
import "./globals.css";
import Providers from './providers'

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className="relative min-h-screen text-text-light dark:text-text-dark bg-background-light dark:bg-background-dark" >
        <script
          dangerouslySetInnerHTML={{
            __html: getInitialThemeScript(),
          }}
        />
        <div className="fixed inset-0 -z-10 opacity-10 bg-gradient-to-b from-background-lightGrad dark:from-background-darkGrad to-background-light dark:to-background-dark"/>

        {/* ✅ Proveedor de sesión dentro de un Client Component */}
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  );
}
