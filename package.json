{
  "scripts": {
    "frontend": "cd frontend-next && npm run dev && cd ..",
    "backend": "cd backend && node server.js && cd ..",
    "dev": "concurrently \"cd frontend && npm start && cd .. \" \"cd backend && node server.js && cd .. \""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "express-session": "^1.18.1"
  }
}
